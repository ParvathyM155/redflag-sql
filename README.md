# redflag-sql
Fraud detection engine built in pure MySQL — 12 fraud patterns caught across 200K simulated fintech transactions using GROUP BY/HAVING, correlated subqueries, and window functions (LAG, ROW_NUMBER). No Python, no ML — just SQL.
